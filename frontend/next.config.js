const path = require('path')

/** @type {import('next').NextConfig} */
const nextConfig = {
  webpack: (config, { isServer }) => {
    // Alias configurations
    config.resolve.alias = {
      ...config.resolve.alias,
      '@': path.resolve(__dirname, 'src'),
      '@generated': path.resolve(__dirname, '../autogenerated/ts')
    }

    // Preserve symlinks

    // Handle global definition for gRPC-web
    config.resolve.fallback = {
      ...config.resolve.fallback,
      global: false,
    }

    // Add google-protobuf to transpiled modules if needed
    config.module.rules.push({
      test: /\.m?js/,
      resolve: {
        fullySpecified: false
      }
    })

    return config
  },
  // Enable SASS if you're using it
  sassOptions: {
    includePaths: [path.join(__dirname, 'src/styles')],
  },
  // If you need to handle specific file types or want to modify page extensions
  pageExtensions: ['tsx', 'ts', 'jsx', 'js'],
}

module.exports = nextConfig
